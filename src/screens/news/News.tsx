import { ScrollView, Text } from 'react-native';
import React from 'react';

const News = () => {
  return (
    <ScrollView>
      <Text>NewsPage</Text>
    </ScrollView>
  );
};

export default News;
